export async function deleteImage(publicId) {
  console.warn(
    "deleteImage: La eliminación de imágenes en Cloudiany requiere un backend. " +
    "Implementa una Cloud Function o endpoint seguro. publicId:",
    publicId
  );
}
